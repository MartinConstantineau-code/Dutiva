import type { ScenarioId } from '@/features/app/views/advisor/advisorScenarios'

/** Hero demo scenarios — breadth without opening the invite-gated app. */
export const LANDING_ADVISOR_SCENARIO_IDS: readonly ScenarioId[] = ['s1', 's3', 's5']
