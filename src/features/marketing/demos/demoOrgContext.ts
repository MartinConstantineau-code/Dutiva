import type { ClauseContext } from '@/features/app/documents/engine'

/** Org profile used for marketing template previews — matches demo-scale Ontario employer. */
export const MARKETING_DEMO_ORG: ClauseContext = {
  jurisdiction: 'ON',
  headcount: 85,
  unionized: false,
}
