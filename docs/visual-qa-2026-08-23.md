# Visual QA — end-to-end app polish (2026-08-23)

PR1 engineering (`cursor/end-to-end-polish` → #201) + PR2 follow-ups
(`cursor/visual-qa-polish`) + PR #203 test-coverage pass (`cursor/test-coverage-gaps`).

## Checklist

| Dimension       | Route / state                              | Light | Dark | EN  | FR                | Demo         | Production            | Result                                    |
| --------------- | ------------------------------------------ | ----- | ---- | --- | ----------------- | ------------ | --------------------- | ----------------------------------------- |
| Marketing       | `/`, `/fr`                                 | ✓     | ✓    | ✓   | ✓ (link present)  | n/a          | n/a                   | Pass — browser smoke on `/`               |
| Search overlay  | ⌘K / Ctrl+K                                | —     | —    | ✓   | ✓ (messages)      | ✓ (Vitest)   | ✓ (Vitest + e2e auth) | Pass — automated                          |
| Advisor home    | `/app/advisor`                             | —     | —    | ✓   | ✓ (starter chips) | ✓ (fixtures) | ✓ (stats hook)        | Pass — Vitest + AdvisorView prod nav      |
| Advisor threads | search → `{ chatId }`                      | —     | —    | ✓   | ✓                 | n/a          | ✓ (hydration)         | Pass — AdvisorView.test prod case         |
| Documents tabs  | hr-library → studio                        | ✓     | —    | ✓   | ✓                 | ✓ gallery    | ✓ redirect            | Pass — route + DocumentsLayout            |
| Communications  | edit + review rail                         | —     | —    | ✓   | ✓ (keys)          | n/a          | ✓                     | Pass — CommunicationsView.test + e2e CRUD |
| Compensation    | edit + market column                       | —     | —    | ✓   | ✓                 | n/a          | ✓                     | Pass — CompensationView.test              |
| Wellbeing       | edit + signals empty                       | —     | —    | ✓   | ✓                 | n/a          | ✓                     | Pass — WellbeingView.test                 |
| Workflows       | production intro                           | —     | —    | ✓   | ✓                 | ✓ fixtures   | ✓ guided only         | Pass — WorkflowsView.test                 |
| Tasks           | production checklist                       | —     | —    | ✓   | ✓ (keys)          | n/a          | ✓                     | Pass — TasksView.test + e2e CRUD          |
| Memory manager  | facts + audit                              | —     | —    | ✓   | ✓ (keys)          | n/a          | ✓                     | Pass — MemoryViews.test + e2e CRUD        |
| Case memory     | resume summary                             | —     | —    | ✓   | ✓ (keys)          | n/a          | ✓                     | Pass — e2e narrative edit                 |
| Shell i18n      | ProductionEmptyState, ChatPane, EntryStage | —     | —    | ✓   | ✓                 | ✓            | ✓                     | Pass — message keys extracted             |
| Tokens          | ToastHost, SignaturePad                    | ✓     | ✓    | n/a | n/a               | ✓            | ✓                     | Pass — semantic tokens                    |
| CSP             | script + style-src                         | n/a   | n/a  | n/a | n/a               | n/a          | n/a                   | Pass — `e2e/csp.spec.ts`                  |

## Browser notes

- **Marketing (`/`):** Loaded locally; dark-mode toggle and FR language link present; cookie banner renders.
- **App workspace:** Unauthenticated sessions redirect to `/app/welcome` — full demo/production visual pass requires a signed-in admin.
- **Production CRUD:** Covered by `e2e/auth/critical-path.spec.ts` when Supabase env is set (see [e2e/README.md](../e2e/README.md) for the full matrix). Woodpecker runs this via `.woodpecker/e2e-auth.yml`.

## Fixes applied

- Visual QA doc (this file).
- E2E auth: production search overlay (`Ctrl+K`), employees/cases/tasks/comms/memory CRUD, documents empty state.
- E2E hermetic: CSP script + style regression (`e2e/csp.spec.ts`).
- CSP: removed `style-src 'unsafe-inline'`; inline React styles refactored to tokens/CSS/SVG.

## Remaining manual pass (owner)

After merge, a signed-in admin should spot-check in both themes:

1. Search a conversation → Advisor opens the persisted thread (not yet auth-e2e'd).
2. Light/dark pixel pass on modules above the Vitest/e2e bar (Advisor chat chrome, Documents Studio generate flow).

Owner gates unchanged — see `docs/TODO.md` (L5 corpus, L6 bands, OA9, OA11).
